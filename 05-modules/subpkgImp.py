## Program to create a subpackage within package and move `module2` in it and modify the import statements in `__init__.py` to use relative imports

import mypackage

print(mypackage.sum(2, 8))
print(mypackage.product(2, 8))
